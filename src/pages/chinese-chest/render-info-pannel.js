import { width, height, cellWidth, riverWidth } from './consts'
import { drawMethod } from './utils'

const renderBorder = (ctx) => {

}

const renderTeam = (ctx) => {

}

const drawPannel = (ctx) => {
  renderBorder(ctx)
  renderTeam(ctx)
}

export default drawPannel
